TODO:
- when a check comes in with a new interval, accept the new interval
- agent should fetch last run for checks from server
- use env WHAZZA_DATA_DIR for server generated key, cert and db files


Done
- agent daemon mode
- agent checks.json config file
- `whazza show` to show health for checks
- generate cert on server
- accept cert using tofu on agent
- `whazza fingerprint` command to show cert fingerprint
- `whazza-agent init ...` to generate config.json
- accept agent on server (whazza register username <token-hash>)
- only accept registered agents on server
